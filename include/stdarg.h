#ifndef STDARG_H
#define STDARG_H

/* va_list is pre-registered by the compiler as a typedef for int.
   va_start, va_arg, va_end are built-in keywords handled by the parser.
   This header exists so that #include <stdarg.h> does not fail. */

#endif
